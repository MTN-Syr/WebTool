# 📖 الدليل التفصيلي: إضافة خدمة جديدة على كتيب MTN

هذا الدليل يشرح الطرق لإضافة خدمة على الموقع:

| للفريق | كيف؟ |
|---|---|
| 📄 **الموظف / غير التقني** | **الطريقة المبسطة (Word فقط)** ↓ الأسهل — ملف Word + أداة سحب وإفلات |
| 👨‍💻 **المشرف (تقني)** | الطريقة ج: استقبال الملف وتحويله ورفعه على GitHub |
| 🌐 **المشرف — صفحات CC Web Tool** | الطريقة د: حفظ صفحة الخدمة ثم `tools/html-to-site.html` |

---

## 📗 الطريقة المبسطة — من ملف Word (.docx) (دقيقة واحدة)

> **هذا هو الأسهل والأسرع** — يكفيك ملف Word واحد و3 نقرات.

### ماذا تحتاج؟
- ملف **Word** (.docx) يحتوي شرح الخدمة + صور توضيحية
- لا تحتاج إنترنت — الأداة تعمل محلياً

### خطوات:

#### ١. جهّز ملف Word
```
✅ اكتب اسم الخدمة في السطر الأول (مثلاً: «خدمة استقبال المكالمات»)
✅ تحته اكتب الشرح التفصيلي
✅ الصق الصور بجانب الفقرات التي تشرحها
✅ اكتب الجداول في Word عادي (جدول → إدراج → جدول)
✅ لا تحتاج تنسيق خاص — كل شيء يُفهم تلقائياً
```

**مثال لمحتوى الملف:**
```
خدمة استقبال المكالمات

وصف الخدمة:
تتيح الخدمة للمشتركين استقبال المكالمات...

طرق الاشتراك:
* أرسل رسالة إلى 999
* اتصل على *100#

الشروط والأحكام:
1. ...
2. ...

[صورة: شاشة الجوال]
```

#### ٢. افتح أداة الإضافة
```
افتح الملف tools/html-to-site.html في المتصفح
(نقرة مزدوجة — يعمل بدون إنترنت)
```

#### ٣. اسحب ملف Word واستخرج
```
اسحب ملف .docx إلى المنطقة الصفراء
(أو اكتب اسم الملف المطلوب في حقل "اسم ملف الخدمة" مثل esim)
↓
اضغط «توليد»
↓
اضغط «⬇ تنزيل ملف الخدمة» — سينزّل ملفاً واحداً فقط (مثل esim.js)
↓
تم! الخدمة جاهزة
```

#### ٤. ضع الملف في المشروع
```
انسخ الملف المُنزّل (مثل esim.js) داخل مجلد data/services/
(إن وُجد ملف بنفس الاسم فاستبدله — يعني التحديث)
خدمة جديدة؟ أضِف اسمها إلى قائمة SERVICE_FILES في data/services/index.js:
   "esim.js",
```
> 💡 **لتعديل خدمة واحدة فقط:** افتح ملفها داخل `data/services/<id>.js`
> وعدّله مباشرة — لا تلمس بقية الملفات.

#### ٥. ارفع على الموقع (اختياري)
```
افتح GitHub Desktop ← ستظهر التغييرات
↓
اضغط Commit ← Push
↓
بعد أقل من دقيقة يصبح الموقع محدّثاً للجميع ✓
```

### ⚠️ ملاحظات مهمة:

| السؤال | الجواب |
|---|---|
| **ماذا لو سحبت الملف مرتين؟** | لا يكرر — يحدث المحتوى بدلاً منه |
| **ماذا لو نسيت الصور؟** | يمكنك سحب الملف مرة أخرى مع الصور لاحقاً |
| **أين أجد الخدمة بعد النشر؟** | الرئيسية أو صفحة التصنيف الذي تنتمي إليه |
| **هل أستطيع تعديل الخدمة؟** | عدّل ملف Word ثم أعد السحب والاستخراج |

---

### الطريقة التفصيلية (للمشرف التقني)

| للفريق | كيف؟ |
|---|---|
| 👨‍💻 **المشرف (تقني)** | الطريقة ج: استقبال الملف وتحويله ورفعه على GitHub |
| 🌐 **المشرف — صفحات CC Web Tool** | الطريقة د: حفظ صفحة الخدمة ثم `tools/html-to-site.html` |
| 📄 **المشرف — ملفات Word** | الطريقة هـ: ملف `.docx` بنص كامل وصور، ثم `tools/html-to-site.html` |

---

## 🧑‍💼 الطريقة أ — عبر فورم إضافة الخدمة (بديلة)

### الخطوات للموظف:
1. افتح صفحة الفورم على الموقع (أو الملف `tools/add-service-form.html`).
2. املأ الحقول:
   - **الأيقونة**: رمز تعبيري مثل `📱` أو `💳`.
   - **الاسم بالعربية** و **بالإنجليزية**: اسم الخدمة باللغتين.
   - **التصنيف**: اختر تصنيفًا موجودًا، أو «تصنيف جديد…» واكتب اسمه وأيقونته.
   - **الملخص**: سطر مختصر يظهر في بطاقة الخدمة.
   - **الشرح**: النص التفصيلي؛ سطر فارغ = فقرة جديدة.
   - **الخطوات**: كل خطوة في سطر مستقل (تُرقَّم تلقائيًا بالموقع).
   - **رابط الفيديو**: رابط يوتيوب، أو اتركه فارغًا ليرفع لاحقًا.
3. اضغط **«معاينة سريعة»** للتأكد من الشكل (اختياري).
4. اضغط **«توليد بيانات الخدمة»** — سيتأكد الفورم من الحقول الناقصة.
5. اضغط **«تنزيل الخدمة (ملف CSV)»** — سيُحفظ ملف باسم `خدمة-جديدة.csv`.
6. أرسل الملف إلى مسؤول الموقع (واتساب/بريد).

⚠️ إذا كتبتَ نفس معرّف خدمة موجودة، فسيحدث **تحديث** لمحتواها بدل إضافة نسخة جديدة.

---

## 🧑‍💼 الطريقة ب — عبر إكسل (مثل الحالي)

1. افتح صفحة أداة التحويل ← زر «قالب Xlsx» لتنزيل قالب مملوء بالأمثلة،
   أو استخدم القالب الجاهز `template/قائمة-الخدمات.csv`.
2. املأ **صفًا واحدًا** لكل خدمة جديدة (او عدّل صف خدمة موجودة مع إبقاء المعرّف نفسه).
3. **الخطوات**: كل خطوة بسطر داخل الخلية (Alt+Enter في إكسل).
4. **الشرح**: سطر فارغ = فقرة جديدة.
5. احفظ الملف وأرسله للمشرف.

---

## 👨‍💻 الطريقة ج — دور المشرف (التحويل + الرفع)

### 1) التحويل
1. افتح صفحة أداة التحويل: `tools/excel-to-site.html`
   (أو رابطها على الموقع: `https://<موقعك>/tools/excel-to-site.html`).
2. **الوضع «دمج»** مفعّل مسبقًا — لن تُحذف أي خدمة حالية (هذا هو الوضع الآمن).
3. اسحب ملف الموظف (CSV أو Xlsx) إلى المنطقة المخصصة.
4. اضغط **«تحويل الملف»**.
5. راجع «نتيجة التحويل»: يجب أن يُظهر مثلاً: `خدمات = 9 · مُضافة 1 · محدّثة 2`.
6. اضغط **«⬇ تنزيل ملفات الخدمات»** — ينزّل ملفاً واحداً لكل خدمة أُضيفت/تعدّلت.

### 2) الرفع على GitHub (مرتيْن أو ثلاث في الدقيقة)
1. **خطوة احتياطية (يُنصح بها أول مرة):** من داخل الأداة زر
   «نسخة احتياطية» ينزّل نسخة من الخدمات الحالية كملفات مجزأة.
2. انسخ الملفات المُنزّلة داخل مجلد `data/services/` في مشروعك (استبدل أي ملف بنفس الاسم).
3. **للخدمات الجديدة فقط:** أضِف اسم الملف إلى قائمة `SERVICE_FILES`
   في `data/services/index.js` (سطر واحد مثل `"esim.js",`).
4. افتح **GitHub Desktop** ← ستظهر تغييرات ← اكتب وصفًا مثل
   `إضافة خدمة جديدة` ← زر **Commit to main** ← زر **Push origin**.
5. بعد أقل من دقيقة يصبح الموقع محدّثًا للجميع ✓

> لا حاجة لأي خطوة بناء أو إعداد — الموقع ثابت ويقرأ `data/services/` مباشرة.

---

## 🌐 الطريقة د — إضافة خدمة من صفحة CC Web Tool المحفوظة (الطريقة المطابقة للخدمات الحالية)

خدمات الموقع الحالية (9 خدمات) مستخرجة من الصفحات المحفوظة داخل `Webtool/WebTool`.
لاستخدام هذه الطريقة لخدمة جديدة:

### 1) حفظ صفحة الخدمة من CC Web Tool
1. افتح صفحة الخدمة في المتصفح.
2. اضغط **Ctrl+S** واختر تنسيق **«صفحة ويب كاملة»** (HTML). يُحفظ ملف `.html` مع مجلد `_files` بجانبه (كلا الملفين ضروريان إن كانت الصفحة تحتوي صورًا/أنماطًا داخلية).

### 2) التحويل عبر أداة HTML
1. افتح `tools/html-to-site.html` بنقرة مزدوجة (يعمل بدون سيرفر).
2. اسحب الملف `.html` المحفوظ إلى **منطقة الإفلات** (أو الصقه في منطقة اللصق).
3. راجع **المعاينة** لكل ملف: العنوان، عدد أحرف الشرح، عدد الخطوات، اكتشاف الفيديو.
4. اضغط **«توليد»** (الدمج يضيف الجديد ويحدّث الموجود تلقائيًا — لا تكرار).
5. اضغط **«⬇ تنزيل ملف الخدمة»** وانقل الملف داخل `data/services/` — وللخدمة الجديدة أضِف اسمها إلى `SERVICE_FILES` في `index.js`.

### 3) الرفع على GitHub
نفس خطوات الطريقة ج (قسم 2): نسخ الملفات داخل `data/services/` ثم GitHub Desktop → Commit → Push.

> **الفيديو:** إن وُجد رابط يوتيوب/Drive/MP4 داخل الصفحة المحفوظة، يلتقطه
> التطبيق تلقائيًا. وإلا يبقى حقل الفيديو فارغًا ويظهر **قسم الفيديو** في
> الصفحة فور الإضافة لاحقًا.
>
> **الصور:** اسحب **ملفات مجلد `_files` مع ملف الصفحة** (حددها كلها معًا)
> لتلتقط الصور تلقائيًا؛ وإلا تُنبّهك الأداة عن عدد الصور الناقصة.
>
> **اللغة:** إن كان محتوى الصفحة عربيًا تُملأ الحقول العربية تلقائيًا
> (وتُنسخ نسخة إنجليزية مختصرة)، والعكس للصفحات الإنجليزية.

---

## 📄 الطريقة هـ — إضافة خدمة عبر ملف Word (.docx) يحتوي الشرح والصور

أفضل طريقة للحصول على **نص كامل + صور** (مثل «الاشتراك بالخط المسبق الدفع.docx»):

### 1) تجهيز ملف Word
1. أنشئ/أعدّ ملف `.docx` يحتوي اسم الخدمة ثم الشرح التفصيلي والصور التوضيحية
   (كل لقطة تُوضع بعد الفقرة التي تشرحها).
2. لا تحتاج أي تنسيق خاص — الجداول والترقيم والصور تُفهم تلقائيًا.

### 2) التحويل عبر الأداة
1. افتح `tools/html-to-site.html` بنقرة مزدوجة (يعمل بدون سيرفر).
2. اسحب ملف `.docx` إلى منطقة الإفلات.
3. راجع المعاينة: عنوان الخدمة، عدد الأحرف، الخطوات، **عدد الجداول** و**عدد الصور** مع مصغّرات لها.
4. اضغط **«توليد»** (الدمج يضيف الجديد ويحدّث الموجود تلقائيًا).
5. اضغط **«⬇ تنزيل ملف الخدمة»** — سينزّل ملفاً واحداً فقط (مثل `esim.js`)؛ انسخه داخل `data/services/` (استبدل أي ملف بنفس الاسم). للخدمة الجديدة أضِف اسمها إلى `SERVICE_FILES` في `data/services/index.js`.

### 3) الرفع على GitHub
نفس خطوات الطريقة ج (قسم 2): نسخ الملفات داخل `data/services/` ثم GitHub Desktop → Commit → Push.

> الصور تُضمَّن داخل ملف الخدمة في `data/services/<id>.js` (Base64) فتظهر في صفحة الخدمة في مواضعها،
> وتعمل حتى بدون إنترنت أو عند الفتح من القرص مباشرة.

---

## 🧪 للتأكد أن كل شيء يعمل (للمشرف)

- افتح `tools/_test-e2e.html` بفتح الملف مباشرة (نقرة مزدوجة) — سيعرض:
  `RESULT: ALL STEPS PASSED ✓`
  ويعني أن رفع CSV وXLSX والدمج وتوليد الكود كلها تعمل.
- الأداة الآن **تعمل بدون إنترنت** (مكتبة SheetJS محلية في `tools/vendor/`).
- لاختبار تحويل صفحات HTML: افتح `tools/html-to-site.html` واسحب أحد ملفات
  `Webtool/WebTool/*.html` — يجب أن تظهر معاينة بشرح عربي وخطوات.
- لاختبار ملفات Word: اسحب «الاشتراك بالخط المسبق الدفع.docx» — يجب أن تظهر
  معاينة بعدد الصور الكامل (10) وصور مصغّرة.

## ❓ مشاكل شائعة

| المشكلة | الحل |
|---|---|
| زر التحويل لا يستجيب | تأكد أنك فتحت الأداة بفتح الملف `tools/excel-to-site.html` مباشرة (نقرة مزدوجة) — لا يحتاج سيرفرًا |
| لم تظهر الخدمة بعد الرفع | تأكد أنك استبدلت ملفات `data/services/` وحذف ذاكرة المتصفح (Ctrl+F5) |
| الخدمة ظهرت لكن بلا فيديو | رابط الفيديو فارغ أو غير صالح — تأكد أنه يبدأ بـ `https://www.youtube.com/watch?v=` أو `https://youtu.be/` |
| دمج بدّل خدمة موجودة | لو قبلت ملفًا ضع فيه معرّف خدمة موجودة، فهذا تعديل مقصود حسب القاعدة؛ للتأكد راجع «نتيجة التحويل» قبل التنزيل |